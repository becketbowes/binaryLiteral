    {{#each articles}}
    <img src="/assets/img/{{image}}" alt="{{imageAlt}}">
    <h3>{{title}}</h3>
    <h4>Written by {{reader.user}} on: {{createdAt}}, keywords: {{keywords}}</h4>
    <section>{{article}}</section>
    <h4><a><button type="button" class="neatasabutton">Neat!</button></a> {{ohNeat}} people thought this was neat. <a href="/literal/{{id}}">{{comments.length}} more comments:</a></h4>
    {{/each}}